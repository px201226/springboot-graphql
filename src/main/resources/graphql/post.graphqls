
type Post {
    id: Float!
    title: String!
    user: User!
    comments: [Comment]
}

input PostInput {
    userId: Float!
    title: String!
}

extend type Query {
    posts: [Post]
}

extend type Mutation {
    addPost(input: PostInput!): Post!
}